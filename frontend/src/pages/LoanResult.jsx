// Placeholder — LoanApply shows result inline; a dedicated result page is unused but reserved
import { Navigate } from "react-router-dom";
export default function LoanResult() { return <Navigate to="/app" replace />; }
